# Full system: the shared disaggregated GPU/update-offload substrate with all
# three contribution-level mechanisms enabled.

SYSTEM_VARIANT_LABEL=full
STORAGE_OWNER_UPDATE_COMPLETION_MODE="${STORAGE_OWNER_UPDATE_COMPLETION_MODE:-decoupled}"
GPU_DYNAMIC_GRAPH_ACCESS_MODE="${GPU_DYNAMIC_GRAPH_ACCESS_MODE:-adaptive}"
GPU_RDMA_SEARCH_PROGRESSION_MODE="${GPU_RDMA_SEARCH_PROGRESSION_MODE:-decoupled}"

source "$EXPERIMENT_DIR/profiles/04_gpu_persistent_gpunetio_common.sh"
